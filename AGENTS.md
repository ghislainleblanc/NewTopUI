# Coding Rules

## SwiftUI

- Define extracted or logically distinct views as separate `View` structs.
- Do not implement subviews as computed properties returning `some View`.
- User visible strings whould always be localized (French and Spanish).
